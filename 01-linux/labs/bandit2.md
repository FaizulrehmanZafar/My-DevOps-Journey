#Bandit level 2 -> 3
Solution & Explanation - 
‘ls’ to list the files and directories in the system
‘cat “spaces in this filename”’ to display it using apostrophes as without it will read each word separately but received error
‘find’ to see how the file is listed in the directory
‘cat “./—spaces in this filename—“‘ this specifies the file name and reads it all as one name instead of individual words
Password - MNk8KNH3Usiio41PRUEoDFPqfxLPlSmx
What I learned - still need to specify using ‘./‘ even if the file name contains spaces in its name as well as the the forward dash
